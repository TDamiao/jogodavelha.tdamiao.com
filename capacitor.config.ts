import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'jogodavelha.tdamiao.com',
  appName: 'jogo da velha rob',
  webDir: 'dist'
};

export default config;
